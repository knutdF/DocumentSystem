name: Externer Webhook Handler
on: repository_dispatch

jobs:
  webhook_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Run script
        run: bash path/to/your/script.sh
